import MainCommissions from './MainCommissions';

function Commissions() {
  return (
    <section className="h-screen flex flex-row">
      <MainCommissions />
    </section>
  );
}

export default Commissions;
